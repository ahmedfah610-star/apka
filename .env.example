# Supabase (etap 2+) — wartości znajdziesz w Project Settings > API w Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Google Places API — zasila ranking weterynarzy/fryzjerow (/specjalisci) realnymi danymi.
# BEZ prefiksu NEXT_PUBLIC_ — klucz uzywany jest tylko po stronie serwera (src/app/api/specjalisci/route.ts).
# Bez tego klucza endpoint zwraca dane demo, aplikacja dziala normalnie.
GOOGLE_PLACES_API_KEY=
